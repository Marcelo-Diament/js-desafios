/**
 * DESAFIO 01
 * Criar uma função que exiba (console.log()) apenas os lançamentos (lançados no mês atual).
 * Formato esperado: um array de objetos com título, descrição, preço e data de lançamento (no formato brasileiro) por produto
 */

/**
 * DESAFIO 02
 * Criar uma função que exiba (console.log()) um array apenas com produtos em promoção. 
 * Para cada produto devemos mostrar uma string única assim: "{TITULO} - de {preco} por apenas {precoPromo}".
 * Ah! Mostrar os valores em reais (R$ 99,00)
 */

/**
 * DESAFIO 03
 * Criar uma função que exiba (console.log()) um objeto com uma chave por categoria.
 * Cada categoria terá como valor um array com os produtos pertencentes a ela.
 * Cada produto será exibido com uma string assim: {titulo} | "{descricao}").
 */

/**
 * DESAFIO 04
 * Criar uma função que exiba uma tabela no console com as colunas:
 * Categoria (título da categoria), Produto (título do produto), Preço (em R$ 99,99), Promoção (true ou false), Desconto (em R$ 99,99 - se não houver desconto, mostrar um hífen)
 */
